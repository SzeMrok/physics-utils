{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Physics Utils","text":"<p>Small physics helper package using Pint.</p>"},{"location":"#mechanics","title":"Mechanics","text":""},{"location":"#physics_utils.mechanics.kinetic_energy","title":"<code>kinetic_energy(m, v)</code>","text":"<p>Calculate the kinetic energy.</p> <p>Parameters:</p> Name Type Description Default <code>m</code> <code>float</code> <p>Mass of the object (in kilograms).</p> required <code>v</code> <code>float</code> <p>Velocity of the object (in meters per second).</p> required <p>Returns:</p> Name Type Description <code>E_k</code> <code>Quantity</code> <p>Kinetic energy (in joules).</p> Source code in <code>src/physics_utils/mechanics.py</code> <pre><code>def kinetic_energy(m: float | pint.Quantity, v: float | pint.Quantity) -&gt; pint.Quantity:\n    \"\"\"\n    Calculate the kinetic energy.\n\n    Args:\n        m (float): Mass of the object (in kilograms).\n        v (float): Velocity of the object (in meters per second).\n\n    Returns:\n        E_k (pint.Quantity): Kinetic energy (in joules).\n    \"\"\"\n\n    if not isinstance(m, _si.Quantity):\n        m = m * _si.kg\n\n    if not isinstance(v, _si.Quantity):\n        v = v * _si.m / _si.s\n\n    return 0.5 * m * v**2\n</code></pre>"},{"location":"#orbital","title":"Orbital","text":""},{"location":"#physics_utils.orbital.escape_velocity","title":"<code>escape_velocity(M, R)</code>","text":"<p>Calculate the escape velocity.</p> <p>Parameters:</p> Name Type Description Default <code>M</code> <code>float</code> <p>Mass of the celestial body (in kilograms).</p> required <code>R</code> <code>float</code> <p>Radius from the center of the celestial body (in meters).</p> required <p>Returns:</p> Name Type Description <code>v_esc</code> <code>Quantity</code> <p>Escape velocity (in meters per second).</p> Source code in <code>src/physics_utils/orbital.py</code> <pre><code>def escape_velocity(M: float | pint.Quantity, R: float | pint.Quantity) -&gt; pint.Quantity:\n    \"\"\"\n    Calculate the escape velocity.\n\n    Args:\n        M (float): Mass of the celestial body (in kilograms).\n        R (float): Radius from the center of the celestial body (in meters).\n\n    Returns:\n        v_esc (pint.Quantity): Escape velocity (in meters per second).\n    \"\"\"\n\n    if not isinstance(M, _si.Quantity):\n        M = M * _si.kg\n\n    if not isinstance(R, _si.Quantity):\n        R = R * _si.m\n\n    return (2 * G * M / R) ** 0.5\n</code></pre>"},{"location":"#physics_utils.orbital.orbital_period","title":"<code>orbital_period(M, a)</code>","text":"<p>Calculate the orbital period.</p> <p>Parameters:</p> Name Type Description Default <code>M</code> <code>float</code> <p>Mass of the celestial body (in kilograms).</p> required <code>a</code> <code>float</code> <p>Semi-major axis of the orbit (in meters).</p> required <p>Returns:</p> Name Type Description <code>T</code> <code>Quantity</code> <p>Orbital period (in seconds).</p> Source code in <code>src/physics_utils/orbital.py</code> <pre><code>def orbital_period(M: float | pint.Quantity, a: float | pint.Quantity) -&gt; pint.Quantity:\n    \"\"\"\n    Calculate the orbital period.\n\n    Args:\n        M (float): Mass of the celestial body (in kilograms).\n        a (float): Semi-major axis of the orbit (in meters).\n\n    Returns:\n        T (pint.Quantity): Orbital period (in seconds).\n    \"\"\"\n\n    if not isinstance(M, _si.Quantity):\n        M = M * _si.kg\n\n    if not isinstance(a, _si.Quantity):\n        a = a * _si.m\n\n    return 2 * math.pi * (a**3 / (G * M)) ** 0.5\n</code></pre>"},{"location":"#physics_utils.orbital.orbital_velocity","title":"<code>orbital_velocity(M, R)</code>","text":"<p>Calculate the orbital velocity.</p> <p>Parameters:</p> Name Type Description Default <code>M</code> <code>float</code> <p>Mass of the celestial body (in kilograms).</p> required <code>R</code> <code>float</code> <p>Radius from the center of the celestial body (in meters).</p> required <p>Returns:</p> Name Type Description <code>v_orb</code> <code>Quantity</code> <p>Orbital velocity (in meters per second).</p> Source code in <code>src/physics_utils/orbital.py</code> <pre><code>def orbital_velocity(M: float | pint.Quantity, R: float | pint.Quantity) -&gt; pint.Quantity:\n    \"\"\"\n    Calculate the orbital velocity.\n\n    Args:\n        M (float): Mass of the celestial body (in kilograms).\n        R (float): Radius from the center of the celestial body (in meters).\n\n    Returns:\n        v_orb (pint.Quantity): Orbital velocity (in meters per second).\n    \"\"\"\n\n    if not isinstance(M, _si.Quantity):\n        M = M * _si.kg\n\n    if not isinstance(R, _si.Quantity):\n        R = R * _si.m\n\n    return (G * M / R) ** 0.5\n</code></pre>"}]}